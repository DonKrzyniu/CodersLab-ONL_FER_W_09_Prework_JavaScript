const numbers = [];
const columns = 4;
const rows = 5;

/**
 * Write your code below!
 */

let push = numbers;
for (let i = 1; i < 5; i + 1);
    for (let j = 1; j < 4; j + 1) {
    push = push + 1;
    console.log(push);
}

//Na tym zakończę bo nie mam pomysłów jak to robić poprawnie.
//Te tablice to póki co zbyt skomplikowane zagadnienia jak dla mnie.